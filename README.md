# Installation Instructions:

Run this docker image -> https://hub.docker.com/r/px4io/px4-sitl-gazebo

Run this software (visualization only, not strictly required) -> https://qgroundcontrol.com/

Create your python environment and install dependencies
```pip install mavsdk```

Run the hello_drone.py script!
